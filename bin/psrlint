#!/usr/bin/env php

<?php

require_once __DIR__."/../vendor/autoload.php";

use function PsrLinter\run;
use function PsrLinter\showResult;
use function PsrLinter\returnExitCode;

$options = getopt("",["path:"]);

$result = run($options["path"]);

showResult($result);
returnExitCode($result);
